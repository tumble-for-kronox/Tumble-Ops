apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.tumble.service.type }}
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ .Release.Name }}-frontend
